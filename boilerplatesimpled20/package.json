{
  "name": "simpled20",
  "version": "2.0.0",
  "description": "CSS compiler for the SimpleD20 system",
  "scripts": {
    "build": "sass src/scss/simpled20.scss css/simpled20.css --style=expanded --no-source-map",
    "watch": "sass src/scss/simpled20.scss css/simpled20.css --style=expanded --source-map --watch"
  },
  "browserslist": [
    "last 3 versions"
  ],
  "author": "Asacolips",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "sass": "^1.53.0"
  }
}